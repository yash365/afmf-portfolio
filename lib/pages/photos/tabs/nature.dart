import 'package:flutter_web/material.dart';

class Nature extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text(
        "Nature photos, coming soon",
        style: TextStyle(color: Colors.white, fontSize: 30.0),
      )),
    );
  }
}
